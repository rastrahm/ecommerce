/** @type {import('next').NextConfig} */
const nextConfig = {
  // Next.js 16.0.3 tiene Turbopack habilitado por defecto
  // Para deshabilitarlo, no configuramos nada en experimental
  // El flag NEXT_PRIVATE_SKIP_TURBO=1 en el script es suficiente
};

module.exports = nextConfig;

